1 --> Get data : 
        --> identify and connect to data sources 
        --> select a storage model
        --> fix performance issues
        --> resolve data import errors
    
    ( Storage mode asked to select when taking data from live database )
    Storage Mode -->
        Import --> data copy paste directly and stored into PowerBI (takes your storage)
        Direct Query --> reference from data stored in different place ( dynamic data )
                         eg , if one user registers its data gets storeed in database , PowerBI is connected
                         to that database and itself too add that row ..( not your storage used)
        Dual --> Combination of both (Import and Direct Query)
    
2 --> Clean , Transform and Load data:
       --> load data means saving the data i.e cleaning is done 
       --> Transform data(in power query editor) is resolve inconsistencies , null values and unexpected values
       --> apply data shape transformations
       --> apply user friendly naming conventions
