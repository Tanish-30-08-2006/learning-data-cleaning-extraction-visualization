So ,  first thing is to get information about every column on dataset , for that we go to 
view--> column quality , column distribution , column profile

from this we get empty (null) values in dataset to take care of so we get our next step here..
for this step we go to TRANSFORM --> replace values --> now we can replace one value with another in that specific column selected..
We write value to find as empty so empty boxes are selected in that selected column and replace with NA(not available) 

now we can trim every column to remove extra spaces user may have entered to keep every string in box formatted and clean as much as possible
TO TRIM-->  Tranform --> format --> trim every row // uppercase or lowercase every row...



Here if by default first row as headers and names of column names are column1 column 2..
then go Home--> select (use first headers as column)

# we do column profile , column distribution , column quality for step 1 for transforming data  

now we do FEATURE EXTRACTION i.e generating new columns or attributes derived from existing ones
 
first step , use pascal shape naming convention for column names i.e Customer Name to CustomerName 
Order Id to OrderId

now to get a new column delimetre based from exisiting one go to Add column --> Extract 

now to see new index column i.e row labels we can select index column in add column section from that
you can add index column starting from 0 , 1 or any custom..

# we can add custom column as well like 18% revenue for gst calculation of something in which we 
can write our own formulas on exsiting columns... eg to calculate total sales from unit price * quantity
# remember to apply the changes on datatype as to change the new datatype of new column created

Now , we can add conditional columns eg for feedback score given from 0 to 5 --> very poor poor below avg.... excellent
so this is a conditional column based on an existing column..

