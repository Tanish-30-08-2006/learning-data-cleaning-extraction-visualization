After tranforming data we do apply changes that loads the data into report view first glance of powerBI where 
reports and dashboards are created 

we can view report view ,  tables view , schema visualizer like supabase where all tables will be there and we can connect them by establishing
relationships by our own (foreign keys and primary keys) ..

now we have 4-5 tables in modelling_dax_data now we have to create relationships from manage relationships in schema view

now we want multiple tables to be connected to a fact table which has foreign keys and dimension tables has informative attributes 

modelling means making relationships mainly user relationships between tables ,  by this we connect data to a single fact table 

while making relationships we avoid many to many and one to one to avoid rebundancies


# Now to get {month day year } wise sales we can do two things 
use  marices and in first one add year and then total sales as values 
second one add month  then total sales as values to get date wise sales 
third one add day then total sales as values to get day wise sales ..

OR WE CAN CREATE A Hierarchy which has year -> month --> date so we will pass hierarchy and total sales as values ...

Now if we want to calculate total revenue with gst  18%    
for this we can go tranfrom data add a new column of revenue calculated from total sales but this will increase our storage data size so we will not make these calculations in power query editor i.e transform data 

OR we can create new measure instead of a whole new actual column in report view itself.. 
dax is just the formula of that measure or new column we will create for different calculations .. 
column is row wise calculation of value made from different columns (18% of total sales ) whereas measure give us a single value like sum of total sales 

if we create new column in report view it will not take space as original data remains same in transform data
in transform data no  new column will be added

now revenue was calculated month wise day wise so we created its new column in report view not in transform data , now if we want to calcuate sum of sales all time we have to create a measure as single number .. 

# use of cards to display measure
to display one measure (sum of total sales or sum of total revenue ) WE CAN USE CARDS to display measures 

# note to change datatype of measures and new columns created in report view 
remember to change datatype of sales , unit price , avg or sum sales from general to decimal number especially when we create any new measure from exisitng attribute from fact table

# conditioned columns ..
now to create some conditions like if total sales for that period is greater than some value then we say sales were high or else low ... so we can know many days , months or years sales were low , we can use hierarchy here for year -- > month -- > day and apply that condition and hierarchy in a matri and check for all days where sales were low .......
for these conditions we use a measure because columns are calcullated columns , it looks at data row by row and labels it low or high forever so if i label rows high and low and group it by month and year it will not calculate sum for that month or year and again label t high or low , columns are labels for every row we cant group it for month and year later
# but 
measure is an temporary attribure which can be calculated for rows and  group of rows for example for month 30 days sum must be calculated and mmust be labelled as high or low measure can do that grouping 

# multiple if else loops --> switch 
here we labelled sales as high or low so 2 possibilities what if high,very high,low,very low i.e multiple if else cases , there we use switches..

so for example regioon grouping , north -> zone 1 ,  south -> zone 2 ...
for this we use switch we know it is a permanent label so we can add temp column in report view itself for region grouping in zones , measure is especially used when labelled rows are grouped in future like in sales label of high and low 

code for new column -->
RegionGrouping = SWITCH(Dim_Customers[Region],"North","Zone 1","South","Zone 2","East","Zone 3","West","Zone 4")

# use of iferror function
for example when we want to check in the column if some row calculation had errors so how can we do that ..
by iferror function we can perform a calculation if it has some error then we can write 0 or string "error" or anything we want so we can look up in the table where any row had errored value 

Check Error = IFERROR(Fact_Sales[TotalSales]/0,0) 
here all rows in this new column check error will be 0 as we pass 0 if error .. as anything/0 will give error..

Check Error = IFERROR(Fact_Sales[TotalSales]/0,"error")
here all rows will show error as message in check error column in fact sales table  as /0 will give error 

